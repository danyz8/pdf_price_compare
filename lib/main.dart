// Main Dart file (short version for demo; replace with full code from the canvas)
import 'package:flutter/material.dart';
void main() => runApp(const MaterialApp(home: Scaffold(body: Center(child: Text('Hello PDF Compare')))));
